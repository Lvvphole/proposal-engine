# Terraform working files — never commit these.
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Variable files may contain secrets.
*.tfvars
*.tfvars.json

# CLI config used for offline/local provider mirrors.
.tfrc
